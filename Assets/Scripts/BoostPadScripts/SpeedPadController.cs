using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedPadController : MonoBehaviour
{
    /*
    public PlayerControllerBoost playerSpeed;
    public float speedBoost;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.CompareTag("Player"))
        {
            speedBoost = playerSpeed.actualSpeed * 2; 
            playerSpeed.GetComponent<Rigidbody>().AddForce(new Vector3(-speedBoost, 0, 0), ForceMode.Impulse);
            
            Debug.Log("Boost:" + speedBoost);
        }
    }
    */
}
